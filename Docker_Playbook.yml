---
- hosts: webservers
  become: true
  tasks: 
    - name: install Docker 
      yum: 
        name: docker
        state: present
    - name: start and enable docker 
      service:
        name: docker
        state: started
        enabled: true


